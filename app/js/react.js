import React, {Component} from 'react'
//import config from './config.json';

class Greeter extends Component{
  render() {
    return (
      <div>
        react测试后面，测试哈希值<br/>
        build文件被删空，但是浏览器还是可以正常显示文件
      </div>
    );
  }
}

export default Greeter